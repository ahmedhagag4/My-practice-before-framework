package JavaBase;

public class ExplainTheImage {

    //JavaBase is the module and it has list of packages
    //packages in blue is can be used outside the module (exported)
    //packages in red in only can be used inside the module (concealed)
}
