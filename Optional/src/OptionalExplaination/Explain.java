package OptionalExplaination;

public class Explain {

    //Optional in a tool to make sure you program not gonna crash on runtime because of a NullException
    //and Optional is used with thing that will (return) something so it will prevent that return from
    //returning null value and crash the program

    //so we use it just to say (this thing) may have a value or not so if not make sure to handle it

    //the Optional main goal is making the developer how reading the code knows that here is a return
    //may return null so be aware of handling it before crashing on runtime
}
